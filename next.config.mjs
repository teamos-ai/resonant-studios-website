/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Marketing site: no remote image domains needed yet (everything is local).
  // Vercel auto-detects this config.
};

export default nextConfig;
